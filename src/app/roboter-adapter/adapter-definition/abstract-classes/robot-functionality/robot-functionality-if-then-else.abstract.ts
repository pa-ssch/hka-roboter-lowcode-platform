// TODO: this must be three different functionalities, because each functionality should have only one sub-list (if, if-else, else)
// the ui will know, that an else and if-else is only allowed after an if or after an else, so there should be no problem, that this elements are not one element
